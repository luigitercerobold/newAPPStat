import React, { useState } from 'react';

import Calendario from '../../../Lib/Component/Calendar'

const Calendar = ({ eventStart, eventFinish, title }) => {
   return (
      <Calendario />
   )
}
export default Calendar

